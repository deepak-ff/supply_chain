name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write
  id-token: write
  packages: write

env:
  GO_VERSION: "1.25"

jobs:
  goreleaser:
    name: Release with GoReleaser
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - name: Verify license keys are configured
        run: |
          if [ -z "${{ secrets.CW_LICENSE_PUBLIC_KEY }}" ]; then
            echo "::error::CW_LICENSE_PUBLIC_KEY repo secret is not set — refusing to cut a release without the license verification key. Generate a keypair with: go run ./cmd/license-gen -keygen"
            exit 1
          fi

      - name: Build dashboard
        run: |
          cd dashboard
          npm ci
          npm run build
          cd ..
          tar -czf dashboard-dist.tar.gz -C dashboard/dist .

      - name: Run GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          distribution: goreleaser
          version: latest
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CW_LICENSE_PUBLIC_KEY: ${{ secrets.CW_LICENSE_PUBLIC_KEY }}
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

  sign-images:
    name: Build & Sign Container Images
    runs-on: ubuntu-latest
    needs: goreleaser

    steps:
      - uses: actions/checkout@v4

      - name: Set lowercase owner
        run: echo "OWNER_LC=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Build and push all-in-one image
        id: push-aio
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          sbom: true
          provenance: mode=max
          build-args: |
            VERSION=${{ github.ref_name }}
            CW_LICENSE_PUBLIC_KEY=${{ secrets.CW_LICENSE_PUBLIC_KEY }}
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/chainwarden:${{ github.ref_name }}
            ghcr.io/${{ env.OWNER_LC }}/chainwarden:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push API-only image
        id: push-api
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./internal/api/Dockerfile
          push: true
          sbom: true
          provenance: mode=max
          build-args: |
            VERSION=${{ github.ref_name }}
            CW_LICENSE_PUBLIC_KEY=${{ secrets.CW_LICENSE_PUBLIC_KEY }}
          tags: |
            ghcr.io/${{ env.OWNER_LC }}/chainwarden-api:${{ github.ref_name }}
            ghcr.io/${{ env.OWNER_LC }}/chainwarden-api:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Sign images (keyless)
        run: |
          cosign sign --yes \
            "ghcr.io/${{ env.OWNER_LC }}/chainwarden@${{ steps.push-aio.outputs.digest }}"
          cosign sign --yes \
            "ghcr.io/${{ env.OWNER_LC }}/chainwarden-api@${{ steps.push-api.outputs.digest }}"

      - name: Release summary
        run: |
          {
            echo "## ChainWarden ${{ github.ref_name }} Released"
            echo ""
            echo "### Quick Start"
            echo '```bash'
            echo "docker run -d --name chainwarden -p 3000:3000 ghcr.io/${{ env.OWNER_LC }}/chainwarden"
            echo '```'
            echo ""
            echo "### Binaries"
            echo "- [Download](https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }})"
            echo ""
            echo "### Container Images"
            echo "- All-in-one: \`ghcr.io/${{ env.OWNER_LC }}/chainwarden:${{ github.ref_name }}\`"
            echo "- API only:   \`ghcr.io/${{ env.OWNER_LC }}/chainwarden-api:${{ github.ref_name }}\`"
            echo ""
            echo "### CLI Install"
            echo '```bash'
            echo "curl -sSfL https://raw.githubusercontent.com/${{ github.repository }}/main/install.sh | bash"
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
